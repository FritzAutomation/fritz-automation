export const metadata = {
  title: 'ROI Calculator | Fritz Automation',
  description: 'Calculate your potential savings from automation.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
